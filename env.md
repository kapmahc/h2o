# For Development

- install go

  ```
  GOPATH=/opt/go
  PATH=$GOPATH/bin:$PATH
  export GOPATH PATH
  ```

- nvm global package path

  ```
  NPM_PACKAGES="${HOME}/.npm-packages"
  PATH="$NPM_PACKAGES/bin:$PATH"
  ```
